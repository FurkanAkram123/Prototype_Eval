# Prototype_Eval
Attached is snippet of code created to evaluate and visualize the data from a survey  which evaluates 3 different prototypes. To run this code you will need the following libraries:

Pandas (install using pip install pandas)
Matlibplot (install using pip install matplotlib)
JSON (no need to install, just import)
